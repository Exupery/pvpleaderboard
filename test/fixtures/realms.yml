<% ["US", "EU"].each do |region| %>
  <% 100.times do |n| %>
  realm<%= region %><%= n %>:
    slug: <%= "realm#{n}" %>
    name: <%= "Realm#{n}" %>
    region: <%= region %>
    battlegroup: <%= "battlegroup#{n % 5}" %>
    timezone: <%= "timezone#{n % 5}" %>
    type: pvp
  <% end %>
<% end %>